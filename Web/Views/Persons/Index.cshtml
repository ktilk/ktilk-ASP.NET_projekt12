@model List<Domain.Aggregate.PersonWithContactCount>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FirstOrDefault().Person.FirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FirstOrDefault().Person.LastName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FirstOrDefault().Person.Height)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FirstOrDefault().Person.Weight)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FirstOrDefault().Person.BirthDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FirstOrDefault().Person.DateCreated)
        </th>
        <th>
            Contact count
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Person.FirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Person.LastName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Person.Height)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Person.Weight)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Person.BirthDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Person.DateCreated)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ContactCount)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Person.PersonID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Person.PersonID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Person.PersonID })
        </td>
    </tr>
}

</table>
